# Reproduction environment

The final analysis environment has not yet been selected. The repository will use either:

- R with an `renv.lock` file, or
- Python with a pinned dependency file.

The choice will be made after confirming the software used for the original replication and the requirements of the extension. Reproduction instructions will be tested from a clean environment before final submission.
